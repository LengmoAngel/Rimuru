v3.8.1
    新增覆盖用户接口的断言机制 增强 V3 库的健壮性 主要针对空指针
    新修改可能导致部分接口执行时长变长 约为一次函数跳转与判断的时长
    修复 spi/softspi 的读取空指针 bug 该处问题可能导致内存访问错误

v3.8.0
    更新了 Flash 接口形式 删除了区块与页的枚举体
    新增 getchar 支持
    修复了一些注释错误
    修改了底层驱动源码 不再使用旧版接口 对用户而言没有影响
    GPS 驱动修改 结构改动 增加了稳定性并且不再会出现指令丢失现象
    修改了 interrupt 的全局开关接口 使得可以传参返回 方便状态嵌套
    修改了 flash 接口 在写入部分新增了中断关闭 避免出现 flash 操作被中断打断
    修改了屏幕的 CS 控制 初始化为高
    新增 KEY 驱动断言 使得设置周期为 0 时触发断言错误
    新增 IMU660RA 驱动

v3.7.0
    完善了许可申明说明与 License 以及 GPL3 的 copy
    注释风格统一全中文形式
    新增了 FIFO 的数据长度设置
    修复了 FIFO 不能写满的 bug
    ENABLE 定义取消 新增 ZF_ENABLE/ZF_DISABLE 与 ZF_TRUE/ZF_FALSE
    新增 IPS/TFT 的字体大小设置 并统一字库 OLED/TPS/TFT 通用
    修改包含关系

v3.6.0
    MDK 工程更新替换使用 5.37 版本
    修复一些注释错误
    编码器引脚分为两个通道枚举体避免写反 并增加定时器匹配检测
	将底层内部结构规整 函数退出位置统一 禁止多 return 结构
	function 中字符串数字转换函数增加冗余保护机制 防止空指针错误
	function 中新增 hex 与 string 的转换函数
	芯片外设层增加引脚检查
	修复 UART 中断开关的 BUG
	调整 FIFO 工作机制 取消中断操作替换为返回值
	修复 debug 中的 log 输出在屏幕显示下不输出的问题
	屏幕显示使用的宏定义函数简化参数 自适应缩放
	设备的串口通信都更新使用 FIFO
	新增 WIFI UART 的驱动 并增加 check link 功能

v3.5.0
    MDK 工程更新替换使用 5.33 版本
    删除 system_delay_us
    软件 IIC 增加 IO 方向切换
    修改部分函数名称 例如 pit_ms_init
    Debug 输出的重定向放在屏幕初始化之后
    屏幕的 spi 断言 bug 修复

v3.4.2
    修改 fsmc 初始化时序
    修改 ips200 并口驱动 兼容新屏幕
    修改 device key 注释

v3.4.1
    修改 camera_init 接口 优化内存占用
    修改摄像头初始化串口使用方式 优化内存占用
    修正部分注释错误
    更新外设引脚注释方式

v3.4.0
    修改 TIMER 定时器部分接口形式 原有 timer_start 功能拆分为 timer_start 与 timer_init
    修正 IIC 部分接口错误的使能操作
    修改 IIC 接口参数形式
    修正错误的 IIC 函数名称
    修正错误的 SPI 函数名称
    修改 FSMC 驱动方式 RD/WR 使用 FSMC 硬件
    时钟变量初值默认 96MHz
    新增 IMU963RA 驱动

v3.3.0
    修改屏幕接口 clear 不再传递参数 新增 full 替换原有 clear
    修复屏幕的 Debug 关联错误问题 该问题会导致断言递归
    新增 Debug Assert 在初始化屏幕后可以先 clear 屏幕然后输出报错
    修改目前初始化采用的返回值设置 初始化报错后直接进入 Debug Assert
    新增断言的使能与禁用接口
    修正更新的 OLED 6*8 字库编译警告的问题
    修复软件 IIC 读写时钟速率有差异的问题
    修正软件 IIC 的部分设备设置的时钟 MDK 目前速率在 320-360KHz IAR 目前速率在 400-420KHz
    修复硬件 IIC 时钟低于 150KHz 会出现 6050 无法连续读取数据的问题 动态调整硬件 IIC 的软延时
    修复硬件 SPI 低速率下通信波形异常的问题
    修复软件 SPI MIOS初始化错误 结构体错误 以及读取数据异常的问题

v3.2.1
    修复因新增的 Debug 输出功能造成的错误中断关闭导致的 Bug

v3.2.0
    将栈大小拓展为 8Kbyte
    新增 Debug 输出部分 Assert 可提通过屏幕输出报错信息
    修正部分注释

V3.1.5
    修复时钟频率设置为120MHz时未加压超频导致的调试问题
    更新添加默认时钟初始化频率未96MHz

V3.1.4
    修复部分宏定义错误 主要是屏幕
    修改无线串口的 string 函数
    修正注释描述

V3.1.3
    修改 ICM20602 速率避免不可预知的错误
    修复 IAR 警告问题

V3.1.2
    修复str_to_int函数的bug
    修正注释
    修改部分编码格式

V3.1.1
    修改工程名称
    删除不必要的文件
    修改文件夹名称

V3.1.0
    新增SDIO驱动
    新增SDCard外设驱动
    新增FATFS文件系统（FF14B）
    修复Soft_iic的ack响应bug
    修改IPS200驱动文件

V3.0.1
    修改工程编译优化等级
    延续版本信息

V3.0.0
    新命名风格开源库实现
    初始版本

v1.0.7
    更正注释
    提交1.0.6中遗漏的推送<中断组设置>
    新增SEEKFREE_FUN功能函数源文件

v1.0.6
    修改中断优先级设置 默认全抢占式优先级
    更新注释

v1.0.5
    新增断言功能接口
    修复DMA2时钟初始化问题
    修改小钻风解压函数名
    修正部分注释
    使用v6编译

v1.0.4
    更新栈大小默认8K
    删除encoder读取时的清空计数
    修复摄像头数据引脚下标计算错误
    修改PWM部分使能

v1.0.3
    更新spi通信函数
    修改us定时函数

v1.0.2
    修改fsmc地址设置时间
    修改部分注释

V1.0.1
    摄像头DMA初始化修改
    修复isr.c部分错误
    更新官方底层

V1.0.0
    初始版本

